apiVersion: v1
kind: Service
metadata:
  labels:
    app: myservice
  name: myservice
spec:
  ports:
    - nodePort: 30945
      port: 8085
      protocol: TCP
      targetPort: 8080
  selector:
    app: myservice
  type: LoadBalancer